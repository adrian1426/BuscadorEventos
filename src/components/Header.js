import React from 'react';

const Header = () =>{
    return(
        <header className="uk-margin">
            <h1 className="uk-text-center">Buscador de eventos AHH</h1>
        </header>
    );
}

export default Header;